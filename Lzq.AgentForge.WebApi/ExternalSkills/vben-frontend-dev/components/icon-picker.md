# IconPicker - 图标选择器

## 用法示例

```typescript
{
  component: 'IconPicker',
  fieldName: 'icon',
  label: '图标',
}
```

## 关键点
- 图标使用 Iconify 名称（如 `'ion:settings-outline'`）
- 通过 `@vben/icons` 的 `IconifyIcon` 组件渲染
